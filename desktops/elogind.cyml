# elogind — Session management daemon (standalone logind without systemd)
#
# BLFS

[package]
name = "elogind"
version = "257.16"
description = "elogind — Standalone logind session manager without full systemd"
license = "LGPL-2.1-only"
groups = ["desktops", "libs"]
release = 1
arch = "x86_64"

[source]
url = "https://github.com/elogind/elogind/archive/refs/tags/v257.16.tar.gz"
sha256 = "3c8146409bfb9daa77f272212c2b0bf0ae727c782ec87ece0d2d25e0e5bf5f4c"
patches = []

[depends]
runtime = ["glibc", "dbus", "polkit", "linux-pam"]
build = ["gcc", "make", "pkgconf", "meson", "ninja"]

[build]
pre_build = ""
configure = """
mkdir -p build && cd build && \
meson setup .. --prefix=/usr --buildtype=release \
  -Dcgroup-controller=elogind
"""
make = "cd build && ninja -j$(nproc)"
check = "cd build && meson test || true"
install = "cd build && DESTDIR=$PKG ninja install"
post_install = ""

[security]
hardening = ["pie", "fullrelro", "fortify", "stackprotector", "bindnow"]
cflags = "-O2 -pipe"
ldflags = "-Wl,--as-needed"
