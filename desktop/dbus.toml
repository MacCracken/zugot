# dbus — D-Bus message bus system for inter-process communication
#
# BLFS

[package]
name = "dbus"
version = "1.16.0"
description = "dbus — D-Bus message bus for inter-process communication"
license = "AFL-2.1"
groups = ["desktop", "libs"]
release = 1
arch = "x86_64"

[source]
url = "https://dbus.freedesktop.org/releases/dbus/dbus-1.16.0.tar.xz"
sha256 = "9f8ca5eb51cbe09951aec8624b86c292990ae2428b41b856e2bed17ec65c8849"
patches = []

[depends]
runtime = ["glibc", "expat"]
build = ["gcc", "make", "pkgconf"]

[build]
pre_build = ""
configure = "./configure --prefix=/usr --sysconfdir=/etc --localstatedir=/var --runstatedir=/run --disable-static"
make = "make -j$(nproc)"
check = "make check || true"
install = "make DESTDIR=$PKG install"
post_install = ""

[security]
hardening = ["pie", "fullrelro", "fortify", "stackprotector", "bindnow"]
cflags = "-O2 -pipe"
ldflags = "-Wl,--as-needed"
