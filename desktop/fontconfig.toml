# Fontconfig — Font configuration and discovery library
# System-wide font matching and configuration
# Note: freetype already exists in recipes/base/
#
# BLFS

[package]
name = "fontconfig"
version = "2.16.0"
description = "Fontconfig — Font configuration and discovery library"
license = "MIT"
groups = ["desktop", "graphics"]
release = 1
arch = "x86_64"

[source]
url = "https://www.freedesktop.org/software/fontconfig/release/fontconfig-2.16.0.tar.xz"
sha256 = "6a33dc555cc9ba8b10caf7695878ef134eeb36d0af366041f639b1da9b6ed220"
patches = []

[depends]
runtime = ["glibc", "freetype", "expat"]
build = ["gcc", "make", "pkgconf", "meson", "ninja"]

[build]
pre_build = ""
configure = """
mkdir -p build && cd build && \
meson setup .. --prefix=/usr --buildtype=release \
  -Ddoc=disabled \
  -Dtests=disabled
"""
make = "cd build && ninja -j$(nproc)"
check = "cd build && meson test || true"
install = "cd build && DESTDIR=$PKG ninja install"
post_install = ""

[security]
hardening = ["pie", "fullrelro", "fortify", "stackprotector", "bindnow"]
cflags = "-O2 -pipe"
ldflags = "-Wl,--as-needed"
